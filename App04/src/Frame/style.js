import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  ag: {
    width: 180,
    height: 180,
    alignSelf: 'center',
    marginTop: 25,
    marginBottom: 15,
    borderWidth: 5,
    backgroundColor: 'grey',
  }
})

export { styles }